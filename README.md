# Basic Express/ Mongoose application with schema #
Boilerplate for a basic Express app using Mongoose with a schema.

## Installation

    $ npm install

## Usage
Run mongodb:
    $ mongod

In different terminal:
    $ npm start

localhost:3000/documents